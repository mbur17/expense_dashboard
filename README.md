# 💸 Куда ушло?

Мини-проект на **Streamlit** для учёта и анализа личных расходов.
Поддерживает ручной ввод операций, управление категориями, фильтрацию и визуализацию расходов по категориям и месяцам.

## Возможности

- Добавление расходов прямо в приложении
- Управление категориями (добавление через отдельный виджет)
- Фильтрация:
  - по периоду
  - по категориям
  - по минимальной сумме расхода
- Аналитика:
  - расходы по категориям (donut chart)
  - расходы по месяцам (bar chart)
- Таблица расходов
- Демонстрационные данные для быстрого старта

## Стек

- Python 3.11+
- Streamlit
- Pandas
- Plotly (для UI-friendly donut)
- Pytest (юнит-тесты сервисного слоя)

## Архитектура

Проект разделён на два слоя:

### `services/`
Чистая бизнес-логика и операции с данными:
- агрегации
- фильтрация
- работа с in-memory state (через `st.session_state`)
- управление категориями

### `ui/`
UI-компоненты Streamlit:
- формы
- таблицы
- графики
- sidebar-виджеты

## Структура проекта

```text
expense_dashboard/
├─ app.py
├─ services/
│  ├─ __init__.py
│  ├─ demo_data.py
│  ├─ data_service.py
│  ├─ categories.py
│  ├─ analytics.py
│  └─ filters.py
├─ ui/
│  ├─ __init__.py
│  ├─ forms.py
│  ├─ filters.py
│  ├─ charts.py
│  ├─ widgets.py
│  └─ tables.py
└─ tests/
   ├─ conftest.py
   ├─ test_analytics.py
   └─ test_filters.py
```

## Установка

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Запуск

```bash
streamlit run app.py
```
Откроется приложение в браузере.

## Демо-данные

При первом запуске приложение инициализирует демонстрационные расходы из DEMO_CSV.
Данные хранятся в памяти сессии (st.session_state) и не записываются на диск.

## Тесты

Юнит-тестами покрыт сервисный слой:

- analytics
- filters

Запуск тестов:

```bash
pytest
```
